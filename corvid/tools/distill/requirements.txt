# OFFLINE-ONLY deps for the distilled-student pipeline (D1-D3 in README.md).
# NONE of this is needed to build or run corvid — the runtime loads student.bin
# with a hand-rolled, dependency-free reader. Do not add these to the app build.
numpy>=1.24
# torch / diffusion / 3DGS deps are intentionally left unpinned here; choose the
# teacher stack at implementation time (see README.md D2). Examples:
# torch>=2.2
# diffusers>=0.27
# (a 3DGS trainer of choice)
